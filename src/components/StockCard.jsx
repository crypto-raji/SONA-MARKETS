// Stocks and tokens share identical card requirements (logo, name, ticker,
// price, change, mini chart, watchlist button) so both are backed by the
// same AssetCard implementation to avoid duplicated markup drifting apart.
export { default } from './AssetCard.jsx';
